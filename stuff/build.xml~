<project>

  <property name="fusion.home" value="${user.home}/fusion"/>
  <property name="fusion.username" value="admin"/>
  <property name="fusion.password" value="password123"/>

  <target name="get">
    <get src="http://localhost:8764/api/roles"
         dest="roles.json"
         username="${fusion.username}"
         password="${fusion.password}"/>
  </target>
</project>
