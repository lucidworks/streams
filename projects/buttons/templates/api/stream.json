{ 
  "created": "{{ stream.created }}",
  "updated": "{{ stream.updated }}",
  "sid": "{{ stream.sid }}",
  "name": "{{ stream.name }}",
  "description": "{{ stream.description }}",
  "distro": "{{ stream.tgzfile }}",
  "fusion_version": "{{ stream.fusion_version }}",
  "github_repo": "{{ stream.github_repo }}",
  "app_stub": "{{ stream.github_repo }}",
  "labs_link": "{{ stream.labs_link }}"
}