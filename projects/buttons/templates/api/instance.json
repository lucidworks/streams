{ 
  "created": "{{ instance.created }}",
  "updated": "{{ instance.updated }}",
  "started": "{{ instance.started }}",
  "expires": "{{ instance.expires }}",
  "secondsleft": "{{ instance.secondsleft }}",
  "sid": "{{ instance.stream.get().sid }}",
  "name": "{{ instance.name }}",
  "topic": "{{ instance.topic }}",
  "region": "{{ instance.region }}",
  "preemptible": "{{ instance.preemptible }}",
  "ip": "{{ instance.ip }}",
  "admin_link": "{{ instance.admin_link }}",
  "app_link": "{{ instance.app_link }}",
  "password": "{{ instance.password }}",
  "status": "{{ instance.status }}",
  "username": "{{ instance.user.get().username }}",
  "template": "{{ instance.stream.get().sid }}"
}